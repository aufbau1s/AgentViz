# Fixture Workspace Contract

This fixture follows `templates/agents/schema.md` and demonstrates how `status` should order multiple runs in the same status group.

Fixture clock: `2026-05-24T16:00:00-04:00`.
